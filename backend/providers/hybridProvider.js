module.exports.serviceMap = (input, deployment) => ({ provider:'Hybrid (Cloud + On-prem)', compute:'Managed cloud containers/Kubernetes + on-prem services', database:'On-prem PostgreSQL HA with cloud read replica/DR', cache:'Redis cloud or Redis Sentinel', queue:'Cloud queue + RabbitMQ bridge', cdn:'Cloudflare / provider CDN', lb:'Cloud LB + VPN/ExpressRoute/Direct Connect', dns:'Global DNS', auth:'Cloud IAM federation + Keycloak', secrets:'Cloud secrets + Vault', monitoring:'Cloud native monitoring + Prometheus/Grafana', storage:'Object storage + MinIO/Ceph', waf:'Cloud WAF + edge proxy' });
