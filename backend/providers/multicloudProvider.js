module.exports.serviceMap = (input, deployment) => ({ provider:'Multi-cloud (AWS + Azure + GCP)', compute:'Primary Kubernetes in best-scored cloud + DR cluster in second cloud', database:'Managed PostgreSQL primary + cross-cloud replica/backup', cache:'Redis managed service per region', queue:'Cloud-native queues with event bridge', cdn:'Cloudflare global edge', lb:'Global DNS / traffic steering', dns:'Cloudflare / Route53', auth:'Federated OIDC / Entra ID / Cognito', secrets:'Secrets Manager + Key Vault / Secret Manager', monitoring:'OpenTelemetry + provider monitoring', storage:'S3-compatible object storage replication', waf:'Cloudflare WAF + provider WAF' });
